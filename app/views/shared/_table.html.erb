
    <tr>
      <th scope="row"><%=name%></th>
      <td><%= quantity %></td>
      <td><%= total_price %></td>
      <td>
        <%= link_to "Modify", edit_recipe_path(@recipe), class: "btn btn-primary" %>
        <%= link_to "Delete Recipe", recipe_path(@recipe), method: :delete, data: { confirm: "Are you sure you want to delete this recipe?" }, class: "btn btn-danger" %>
      </td>
    </tr>
 